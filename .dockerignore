# Build output from host (we compile inside the container)
target/

# Nix files — not used by the Dockerfile build
flake.nix
flake.lock
default.nix
shell.nix
nix/
result/

# Docs and examples
doc/
fuzz/
script/
CONTRIBUTING.md
SECURITY.md

# Git history (not needed in build context)
.git/

# Non-source noise
.vscode/
.idea/
*.swp
*.swo
*~
